# 华师课程表 — Plan 2：桌面小组件（Glance）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为华师课程表实现 3 个 Jetpack Glance 桌面小组件：**下一节课 2×1**（课程名 + 地点 + 开始时间/倒计时）、**今日课程 4×2**（今日课程列表 + 节次时间段）、**周课表 4×3**（迷你周网格，仅渲染当前周有课，尊重单双周 WeekPattern）。主题跟随 App（Claude/OpenCode，读 DataStore）；课程/作息/学期/主题变动通过广播刷新；数据缺失显示占位文案，不崩溃。

**Architecture:** 沿用单模块分层。纯渲染模型与格式化逻辑放 `ui/widget/WidgetFormatter.kt`（只依赖 domain 模型 + java.time，可 JVM 单测）；`ui/widget/WidgetDataReader.kt` 在 `provideGlance`（suspend 协程）里直读主 Room 库（`ScheduleDatabase`）+ DataStore（主题/学期/激活作息）；每个组件 = 一个 `GlanceAppWidget`（渲染）+ 一个 `GlanceAppWidgetReceiver`（Manifest 注册）。

**Tech Stack:** Kotlin 2.0.21 · Jetpack Compose (BOM 2024.10.01) · **androidx.glance:glance-appwidget:1.1.1**（compileSdk 35 兼容；Glance 1.1.x 已内置 compose 1.7 运行时，与 BOM 2024.10.01 对齐）· Room 2.6.1 · DataStore 1.1.1 · minSdk 26 · compileSdk 35 · AGP 8.7.3 · Gradle 8.11.1

**构建环境事实（务必遵守）：**
- Gradle 不在 PATH，用：`export PATH="$PATH:/c/Users/huang/gradle-install/gradle-8.11.1/gradle-8.11.1/bin"`
- `local.properties` 的 `sdk.dir=C:\\Users\\huang\\Android\\Sdk`
- JDK 21（Zulu，系统默认）
- 首次构建需联网拉依赖（google()/mavenCentral()），Glance 1.1.1 需从 mavenCentral 拉取；若超时参考 core Plan Task 1 的阿里云镜像备选

**前置依赖（务必先完成）：**
- core Plan `2026-08-14-core-app.md` 的 **Task 5**（主题 Palette：`ClaudePalette`/`OpenCodePalette`/`AppThemeType`）、**Task 6**（Room 实体/DAO/`ScheduleDatabase`）、**Task 7**（`SettingsDataStore` + Repository）、**Task 8**（Hilt `DataModule`）需已实现。
- 本 Plan Task 3 会把 core Task 8 的 `DataModule.provideDb` 改为委托到 `DbProvider` 单例（保证 App 与小组件共用一个 Room 实例，避免双实例缓存不一致）；若 core Task 8 尚未实现，实现时应直接采用本 Plan Task 3 给出的 DataModule 形态。
- 本 Plan Task 6 会修改 core Task 7 产出的 3 个 Repository 实现文件（注入 Context + 数据变更后发广播）。

**参考源码：** `D:\dingding\shiguangschedule-ref`（拾光 Apache-2.0）。对照点：
- 数据模型：`data/db/widget/`（拾光为小组件建了独立 widget 库 + `WidgetDataSynchronizer` 同步器）。
- 刷新：`widget/WidgetUpdateHelper.kt`（`updateAllWidgets` 统一分发）+ Manifest 内 `updatePeriodMillis`。
- Manifest：`app/src/main/AndroidManifest.xml` 里 5 个 widget receiver，`android:exported="false"` + `android.appwidget.action.APPWIDGET_UPDATE` + `meta-data android.appwidget.provider`。

**数据来源决策（重要）：** 设计文档 §7 要求「`provideGlance` 直读 Room + DataStore（主题）」。Glance 默认与 App 同进程运行，直接读主库 `ScheduleDatabase` 无需维护同步器，因此本 Plan **默认直读主库，不建独立 widget 库**。拾光之所以建独立 widget 库，是因为其主库结构复杂（proto 序列化、多课程表、跳课日期、WorkManager 周期同步）；本 MVP 主库结构简单（Course/TimeTable/Period/Semester），直读即可，省去同步链路。若后续发现渲染性能问题（跨进程运行 Glance 时直读主库变慢）再引入独立库，届时按拾光 `WidgetDataSynchronizer` 模式实现。

---

## File Structure

```
D:\dingding\schedule\
  gradle/libs.versions.toml                        # [Modify] + glance 版本与库
  app/build.gradle.kts                             # [Modify] + implementation(libs.androidx.glance.appwidget)
  app/src/main/
    AndroidManifest.xml                            # [Modify] + 4 个 receiver
    res/layout/widget_loading.xml                  # 初始加载占位布局（provider initialLayout 用）
    res/xml/next_class_widget.xml                  # 下一节课 2×1 provider 配置
    res/xml/today_widget.xml                       # 今日课程 4×2 provider 配置
    res/xml/week_widget.xml                        # 周课表 4×3 provider 配置
    res/values/strings.xml                         # [Modify] + 3 个组件名
    java/com/scnu/schedule/
      data/db/DbProvider.kt                        # 主库单例（App 与小组件共用）
      data/di/DataModule.kt                        # [Modify] provideDb 委托 DbProvider
      data/repository/CourseRepositoryImpl.kt      # [Modify] Task 6 注入 Context + 发刷新广播
      data/repository/TimeTableRepositoryImpl.kt   # [Modify] 同上
      data/repository/SettingsRepositoryImpl.kt    # [Modify] 同上
      ScheduleApp.kt                               # [Modify] Task 6 种子后发广播
      ui/widget/
        NextClassWidget.kt                         # 下一节课 2×1（含内容 Composable）
        NextClassWidgetReceiver.kt
        TodayWidget.kt                             # 今日课程 4×2（含内容 Composable）
        TodayWidgetReceiver.kt
        WeekWidget.kt                              # 周课表 4×3（含内容 Composable）
        WeekWidgetReceiver.kt
        WidgetModels.kt                            # 组件渲染模型（纯 Kotlin）
        WidgetFormatter.kt                         # 纯逻辑：domain 数据 → 渲染模型
        WidgetDataReader.kt                        # 直读主库 + DataStore，出 WidgetData 快照
        WidgetUpdateNotifier.kt                    # 数据变更广播 + 统一刷新
        WidgetRefreshReceiver.kt                   # Manifest 注册的刷新广播接收器
  app/src/test/java/com/scnu/schedule/ui/widget/
    WidgetFormatterTest.kt                         # 纯逻辑单测
```

**构建命令模板（每个 Task 用）：**
```bash
export PATH="$PATH:/c/Users/huang/gradle-install/gradle-8.11.1/gradle-8.11.1/bin"
cd /d/dingding/schedule
gradle :app:assembleDebug          # 编译
gradle :app:testDebugUnitTest      # 单测
gradle :app:testDebugUnitTest --tests "com.scnu.schedule.ui.widget.WidgetFormatterTest"   # 单测过滤
```

> Glance API 提示：Glance 的 UI 组合层难以 JVM 单测，本 Plan 采用「纯逻辑（`WidgetFormatter`）TDD + 组件编译冒烟」策略。下文出现少量不确定的 Glance API 签名处标注 `[VERIFY]`，控制器复核时按该标记核对，编译报错时按报错就近修正（Glance 1.1.1 的导入包名以 `androidx.glance.*` 为准）。

---

### Task 1: Glance 依赖接入 + 「下一节课」hello 冒烟（验证接线）

本 Task 只做接线证明：加依赖、注册第一个 receiver、渲染固定文本。真实数据在 Task 3 接入。

**Files:**
- Modify: `gradle/libs.versions.toml`
- Modify: `app/build.gradle.kts`
- Modify: `app/src/main/res/values/strings.xml`
- Create: `app/src/main/res/layout/widget_loading.xml`
- Create: `app/src/main/res/xml/next_class_widget.xml`
- Modify: `app/src/main/AndroidManifest.xml`
- Create: `app/src/main/java/com/scnu/schedule/ui/widget/NextClassWidget.kt`
- Create: `app/src/main/java/com/scnu/schedule/ui/widget/NextClassWidgetReceiver.kt`

- [x] **Step 1: 版本目录加 glance** `gradle/libs.versions.toml`

在 `[versions]` 追加一行：
```toml
glance = "1.1.1"
```
在 `[libraries]` 追加一行：
```toml
androidx-glance-appwidget = { group = "androidx.glance", name = "glance-appwidget", version.ref = "glance" }
```

- [x] **Step 2: app 依赖加 glance** `app/build.gradle.kts`

在 `dependencies { ... }` 里（`implementation(libs.androidx.core.ktx)` 附近）加：
```kotlin
    implementation(libs.androidx.glance.appwidget)
```
（`glance-appwidget` 会传递依赖 `androidx.glance:glance` 核心，`androidx.glance.layout.*` / `androidx.glance.text.*` 均可直接使用。）

- [x] **Step 3: 加组件名字符串** `app/src/main/res/values/strings.xml`

```xml
<resources>
    <string name="app_name">华师课程表</string>
    <string name="widget_name_next_class">下一节课</string>
    <string name="widget_name_today">今日课程</string>
    <string name="widget_name_week">周课表</string>
</resources>
```

- [x] **Step 4: 写初始加载占位布局** `app/src/main/res/layout/widget_loading.xml`

```xml
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAF9F5"
    android:gravity="center"
    android:orientation="vertical">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="加载中…"
        android:textColor="#6C6A64"
        android:textSize="12sp" />
</LinearLayout>
```

- [x] **Step 5: 写「下一节课」provider 配置** `app/src/main/res/xml/next_class_widget.xml`

```xml
<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="40dp"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_loading"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen" />
```
> `updatePeriodMillis` 系统下限为 30 分钟（1800000），倒计时以渲染时刻为准、每半小时重算一次即可满足 MVP；精确到分钟的 tick 见 Task 6 末尾的可选增强说明。

- [x] **Step 6: Manifest 注册 receiver** `app/src/main/AndroidManifest.xml`

在 `<application>` 内、`<activity>` 之后加：
```xml
        <receiver
            android:name=".ui.widget.NextClassWidgetReceiver"
            android:label="@string/widget_name_next_class"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_class_widget" />
        </receiver>
```
> `APPWIDGET_UPDATE` 由系统发出，receiver 声明 `android:exported="false"` 正确（参考拾光 Manifest 全部 widget receiver 均 exported=false）。

- [x] **Step 7: 写 hello 冒烟组件** `app/src/main/java/com/scnu/schedule/ui/widget/NextClassWidget.kt`

```kotlin
package com.scnu.schedule.ui.widget

import android.content.Context
import androidx.glance.GlanceId
import androidx.glance.appwidget.GlanceAppWidget
import androidx.glance.appwidget.provideContent
import androidx.glance.layout.Box
import androidx.glance.layout.Modifier
import androidx.glance.layout.fillMaxSize
import androidx.glance.text.Text

/** 下一节课 2×1。Task 1 冒烟：固定文本；Task 3 接入真实数据。 */
class NextClassWidget : GlanceAppWidget() {
    override suspend fun provideGlance(context: Context, id: GlanceId) {
        provideContent {
            Box(Modifier.fillMaxSize()) {
                Text("下一节课")
            }
        }
    }
}
```

- [x] **Step 8: 写 receiver** `app/src/main/java/com/scnu/schedule/ui/widget/NextClassWidgetReceiver.kt`

```kotlin
package com.scnu.schedule.ui.widget

import androidx.glance.appwidget.GlanceAppWidget
import androidx.glance.appwidget.GlanceAppWidgetReceiver

class NextClassWidgetReceiver : GlanceAppWidgetReceiver() {
    override val glanceAppWidget: GlanceAppWidget = NextClassWidget()
}
```

- [x] **Step 9: 编译验证**

Run:
```bash
export PATH="$PATH:/c/Users/huang/gradle-install/gradle-8.11.1/gradle-8.11.1/bin"
cd /d/dingding/schedule
gradle :app:assembleDebug
```
Expected: `BUILD SUCCESSFUL`。首次会拉取 glance 依赖（较慢属正常）。

- [x] **Step 10: Commit**

```bash
cd /d/dingding/schedule
git add -A
git commit -m "feat(widget): Glance 依赖 + 下一节课 2×1 hello 冒烟组件"
```

---

### Task 2: WidgetFormatter 纯逻辑（TDD）

先写失败测试 → 确认编译失败 → 实现 `WidgetModels.kt` + `WidgetFormatter.kt` → 全绿。

**Files:**
- Test: `app/src/test/java/com/scnu/schedule/ui/widget/WidgetFormatterTest.kt`
- Create: `app/src/main/java/com/scnu/schedule/ui/widget/WidgetModels.kt`
- Create: `app/src/main/java/com/scnu/schedule/ui/widget/WidgetFormatter.kt`

- [x] **Step 1: 写失败测试** `WidgetFormatterTest.kt`

```kotlin
package com.scnu.schedule.ui.widget

import com.scnu.schedule.domain.model.Course
import com.scnu.schedule.domain.model.Period
import com.scnu.schedule.domain.model.Semester
import com.scnu.schedule.domain.model.WeekKind
import com.scnu.schedule.domain.model.WeekPattern
import org.junit.Assert.assertEquals
import org.junit.Assert.assertNull
import org.junit.Test
import java.time.LocalDate
import java.time.LocalDateTime

class WidgetFormatterTest {
    // 2026-08-31 是周一（与 core WeekCalculatorTest 一致）
    private val semester = Semester(startDate = LocalDate.of(2026, 8, 31), totalWeeks = 20)

    private fun course(name: String, day: Int, start: Int, end: Int, kind: WeekKind = WeekKind.ALL) =
        Course(name = name, dayOfWeek = day, startPeriod = start, endPeriod = end,
            weekPattern = WeekPattern(kind, 1, 16))

    private val periods = listOf(
        Period(periodIndex = 1, startMinute = 510, endMinute = 550), // 08:30–09:10
        Period(periodIndex = 2, startMinute = 560, endMinute = 600), // 09:20–10:00
        Period(periodIndex = 5, startMinute = 870, endMinute = 910), // 14:30–15:10
    )

    // ---- nextClass ----

    @Test fun `next class picks nearest upcoming`() {
        val now = LocalDateTime.of(2026, 9, 1, 8, 0) // 周二 第1周
        val next = WidgetFormatter.nextClass(
            listOf(course("高数", 2, 1, 2), course("体育", 2, 5, 5)), periods, semester, now)
        assertEquals("高数", next!!.name)
        assertEquals(30, next.minutesUntil) // 8:00 -> 8:30
    }

    @Test fun `next class skips already started`() {
        val now = LocalDateTime.of(2026, 9, 1, 9, 0) // 高数 8:30 已开始
        val next = WidgetFormatter.nextClass(
            listOf(course("高数", 2, 1, 2), course("体育", 2, 5, 5)), periods, semester, now)
        assertEquals("体育", next!!.name)
        assertEquals(330, next.minutesUntil) // 9:00 -> 14:30
    }

    @Test fun `next class null when all passed`() {
        val now = LocalDateTime.of(2026, 9, 1, 15, 0)
        assertNull(WidgetFormatter.nextClass(
            listOf(course("高数", 2, 1, 2), course("体育", 2, 5, 5)), periods, semester, now))
    }

    @Test fun `next class respects single double week`() {
        val now = LocalDateTime.of(2026, 9, 2, 8, 0) // 周三 第1周(单周)
        val odd = course("离散", 3, 1, 1, WeekKind.ODD)
        val even = course("大物", 3, 1, 1, WeekKind.EVEN)
        val next = WidgetFormatter.nextClass(listOf(odd, even), periods, semester, now)
        assertEquals("离散", next!!.name)
    }

    // ---- todayEntries ----

    @Test fun `today entries filtered sorted with time range`() {
        val today = LocalDate.of(2026, 9, 1) // 周二
        val entries = WidgetFormatter.todayEntries(
            listOf(course("体育", 2, 5, 5), course("高数", 2, 1, 2), course("英语", 3, 1, 2)),
            periods, semester, today)
        assertEquals(2, entries.size)
        assertEquals("高数", entries[0].name)
        assertEquals(510, entries[0].startMinute)
        assertEquals(600, entries[0].endMinute) // 第1-2节 -> 08:30–10:00
        assertEquals("体育", entries[1].name)
        assertEquals(870, entries[1].startMinute)
    }

    // ---- weekGrid ----

    @Test fun `week grid filters odd even weeks`() {
        val today = LocalDate.of(2026, 8, 31) // 周一 第1周(单周)
        val odd = course("离散", 1, 1, 2, WeekKind.ODD)
        val even = course("大物", 1, 1, 2, WeekKind.EVEN)
        val every = course("体育", 2, 5, 5)
        val grid = WidgetFormatter.weekGrid(listOf(odd, even, every), semester, today)
        assertEquals(7, grid.days.size)
        assertEquals(listOf("一", "二", "三", "四", "五", "六", "日"), grid.days.map { it.label })
        assertEquals(listOf("离散"), grid.days[0].courses.map { it.name })
        assertEquals(listOf("体育"), grid.days[1].courses.map { it.name })
        assertNull(grid.days[2].courses.firstOrNull()) // 周三无课
    }

    @Test fun `week grid caps rows`() {
        val today = LocalDate.of(2026, 9, 1) // 周二
        val courses = (1..5).map { course("课$it", 2, it, it) }
        val grid = WidgetFormatter.weekGrid(courses, semester, today)
        assertEquals(3, grid.days[1].courses.size) // maxRows 默认 3
    }

    // ---- helpers ----

    @Test fun `countdown text`() {
        assertEquals("正在上课", WidgetFormatter.countdownText(0))
        assertEquals("正在上课", WidgetFormatter.countdownText(-5))
        assertEquals("5 分钟后", WidgetFormatter.countdownText(5))
        assertEquals("1 小时 15 分后", WidgetFormatter.countdownText(75))
    }

    @Test fun `hhmm formatting`() {
        assertEquals("08:30", WidgetFormatter.hhmm(510))
        assertEquals("14:30", WidgetFormatter.hhmm(870))
    }

    @Test fun `date label`() {
        assertEquals("9月1日", WidgetFormatter.dateLabel(LocalDate.of(2026, 9, 1)))
    }
}
```

- [x] **Step 2: 运行确认失败**

Run: `gradle :app:testDebugUnitTest --tests "com.scnu.schedule.ui.widget.WidgetFormatterTest"`
Expected: 编译失败（`WidgetFormatter` 未定义）。

- [x] **Step 3: 写渲染模型** `WidgetModels.kt`

```kotlin
package com.scnu.schedule.ui.widget

/** 下一节课 2×1 渲染模型 */
data class NextClassModel(
    val name: String,
    val location: String,
    val startMinute: Int,
    val minutesUntil: Int,
)

/** 今日一条课程 */
data class TodayEntry(
    val startMinute: Int,
    val endMinute: Int,
    val name: String,
    val location: String,
)

/** 今日课程 4×2 渲染模型 */
data class TodayWidgetModel(
    val dateLabel: String,
    val entries: List<TodayEntry>,
)

/** 周课表一个格子 */
data class CourseCell(val name: String, val colorIndex: Int)

/** 周课表一列 = 一天 */
data class DayColumn(val label: String, val courses: List<CourseCell>)

/** 周课表 4×3 渲染模型 */
data class WeekGridModel(val days: List<DayColumn>)
```

- [x] **Step 4: 写纯逻辑** `WidgetFormatter.kt`

```kotlin
package com.scnu.schedule.ui.widget

import com.scnu.schedule.domain.logic.WeekCalculator
import com.scnu.schedule.domain.model.Course
import com.scnu.schedule.domain.model.Period
import com.scnu.schedule.domain.model.Semester
import java.time.LocalDate
import java.time.LocalDateTime

/**
 * 小组件纯逻辑：把 domain 数据格式化为渲染模型。
 * 只依赖 domain 模型 + java.time，无 Android 依赖，可 JVM 单测。
 */
object WidgetFormatter {
    private val dayLabels = listOf("一", "二", "三", "四", "五", "六", "日")

    /** 最近一节未开始的课（今天内）；没有返回 null */
    fun nextClass(
        courses: List<Course>,
        periods: List<Period>,
        semester: Semester,
        now: LocalDateTime,
    ): NextClassModel? {
        val today = now.toLocalDate()
        val week = WeekCalculator.currentWeek(semester.startDate, today)
        val minuteNow = now.hour * 60 + now.minute
        val periodByIndex = periods.associateBy { it.periodIndex }
        return courses
            .filter { it.dayOfWeek == today.dayOfWeek.value && it.weekPattern.contains(week) }
            .mapNotNull { c ->
                val p = periodByIndex[c.startPeriod] ?: return@mapNotNull null
                if (p.startMinute > minuteNow) {
                    NextClassModel(c.name, c.location, p.startMinute, p.startMinute - minuteNow)
                } else null
            }
            .minByOrNull { it.startMinute }
    }

    /** 今日全部课程，按开始时间升序；时间段取第一节起点到最后一节终点 */
    fun todayEntries(
        courses: List<Course>,
        periods: List<Period>,
        semester: Semester,
        today: LocalDate,
    ): List<TodayEntry> {
        val week = WeekCalculator.currentWeek(semester.startDate, today)
        val periodByIndex = periods.associateBy { it.periodIndex }
        return courses
            .filter { it.dayOfWeek == today.dayOfWeek.value && it.weekPattern.contains(week) }
            .mapNotNull { c ->
                val start = periodByIndex[c.startPeriod] ?: return@mapNotNull null
                val end = periodByIndex[c.endPeriod] ?: return@mapNotNull null
                TodayEntry(start.startMinute, end.endMinute, c.name, c.location)
            }
            .sortedBy { it.startMinute }
    }

    /** 迷你周网格：每列一天，按节次升序，最多 maxRows 门课（含单双周过滤） */
    fun weekGrid(
        courses: List<Course>,
        semester: Semester,
        today: LocalDate,
        maxRows: Int = 3,
    ): WeekGridModel {
        val week = WeekCalculator.currentWeek(semester.startDate, today)
        val days = (1..7).map { day ->
            DayColumn(
                label = dayLabels[day - 1],
                courses = courses
                    .filter { it.dayOfWeek == day && it.weekPattern.contains(week) }
                    .sortedBy { it.startPeriod }
                    .take(maxRows)
                    .map { CourseCell(it.name, it.colorIndex) },
            )
        }
        return WeekGridModel(days)
    }

    fun countdownText(minutes: Int): String = when {
        minutes <= 0 -> "正在上课"
        minutes < 60 -> "${minutes} 分钟后"
        else -> "${minutes / 60} 小时 ${minutes % 60} 分后"
    }

    fun hhmm(minute: Int): String = "%02d:%02d".format(minute / 60, minute % 60)

    fun dateLabel(date: LocalDate): String = "${date.monthValue}月${date.dayOfMonth}日"
}
```

- [x] **Step 5: 运行测试确认通过**

Run: `gradle :app:testDebugUnitTest --tests "com.scnu.schedule.ui.widget.WidgetFormatterTest"`
Expected: PASS（11 个测试全绿）。

- [x] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(widget): WidgetFormatter 渲染纯逻辑 + 单测（下一节/今日/周网格）"
```

---

### Task 3: 数据读取层 + 「下一节课」接真实数据（主题跟随）

**Files:**
- Create: `app/src/main/java/com/scnu/schedule/data/db/DbProvider.kt`
- Create: `app/src/main/java/com/scnu/schedule/ui/widget/WidgetDataReader.kt`
- Modify: `app/src/main/java/com/scnu/schedule/data/di/DataModule.kt`
- Modify: `app/src/main/java/com/scnu/schedule/ui/widget/NextClassWidget.kt`

- [x] **Step 1: 写主库单例** `data/db/DbProvider.kt`

```kotlin
package com.scnu.schedule.data.db

import android.content.Context
import androidx.room.Room

/**
 * 主库单例访问器。App 与小组件同进程，共用这一个实例，
 * 避免「App 的 Hilt 实例」与「小组件自建实例」双实例缓存不一致。
 */
object DbProvider {
    @Volatile
    private var instance: ScheduleDatabase? = null

    fun schedule(context: Context): ScheduleDatabase = instance ?: synchronized(this) {
        instance ?: Room.databaseBuilder(
            context.applicationContext,
            ScheduleDatabase::class.java,
            "schedule.db",
        ).build().also { instance = it }
    }
}
```

- [x] **Step 2: 写数据读取器** `ui/widget/WidgetDataReader.kt`

```kotlin
package com.scnu.schedule.ui.widget

import android.content.Context
import com.scnu.schedule.data.db.CourseEntity
import com.scnu.schedule.data.db.DbProvider
import com.scnu.schedule.data.db.TimeTableWithPeriods
import com.scnu.schedule.data.prefs.SettingsDataStore
import com.scnu.schedule.domain.model.Course
import com.scnu.schedule.domain.model.Period
import com.scnu.schedule.domain.model.Semester
import com.scnu.schedule.domain.model.TimeTable
import com.scnu.schedule.ui.theme.AppThemeType
import kotlinx.coroutines.flow.first

/** 小组件一次渲染所需的最小数据快照 */
data class WidgetData(
    val theme: AppThemeType,
    val semester: Semester,
    val courses: List<Course>,
    val activeTimetable: TimeTable?,
)

object WidgetDataReader {
    /**
     * 直读主 Room 库 + DataStore。
     * 任一步失败返回 null（组件侧显示占位，不崩溃）。
     */
    suspend fun load(context: Context): WidgetData? = runCatching {
        val db = DbProvider.schedule(context)
        val settings = SettingsDataStore(context)
        val theme = settings.theme.first()
        val semester = settings.semester.first()
        val courses = db.courseDao().observeAll().first().map { it.toDomain() }
        val tts = db.timeTableDao().observeAll().first()
        val activeId = settings.activeTimeTableId.first()
        val activeTimetable = tts.firstOrNull { it.timetable.id == activeId }?.toDomain()
            ?: tts.firstOrNull()?.toDomain()
        WidgetData(theme, semester, courses, activeTimetable)
    }.getOrNull()

    private fun CourseEntity.toDomain() =
        Course(id, name, teacher, location, dayOfWeek, startPeriod, endPeriod, weekPattern, colorIndex)

    private fun TimeTableWithPeriods.toDomain() =
        TimeTable(
            timetable.id, timetable.name, timetable.isDefault,
            periods.sortedBy { it.periodIndex }
                .map { Period(timetable.id, it.periodIndex, it.startMinute, it.endMinute) },
        )
}
```

- [x] **Step 3: DataModule 委托单例** `data/di/DataModule.kt`

把 `provideDb` 改为委托 `DbProvider`（其余不变）：
```kotlin
package com.scnu.schedule.data.di

import android.content.Context
import androidx.room.Room
import com.scnu.schedule.data.db.CourseDao
import com.scnu.schedule.data.db.DbProvider
import com.scnu.schedule.data.db.ScheduleDatabase
import com.scnu.schedule.data.db.SemesterDao
import com.scnu.schedule.data.db.TimeTableDao
import com.scnu.schedule.data.prefs.SettingsDataStore
import dagger.Module
import dagger.Provides
import dagger.hilt.InstallIn
import dagger.hilt.android.qualifiers.ApplicationContext
import dagger.hilt.components.SingletonComponent
import javax.inject.Singleton

@Module
@InstallIn(SingletonComponent::class)
object DataModule {
    @Provides @Singleton fun provideDb(@ApplicationContext ctx: Context): ScheduleDatabase =
        DbProvider.schedule(ctx)

    @Provides fun provideCourseDao(db: ScheduleDatabase): CourseDao = db.courseDao()
    @Provides fun provideTimeTableDao(db: ScheduleDatabase): TimeTableDao = db.timeTableDao()
    @Provides fun provideSemesterDao(db: ScheduleDatabase): SemesterDao = db.semesterDao()
    @Provides @Singleton fun provideSettingsDataStore(@ApplicationContext ctx: Context): SettingsDataStore =
        SettingsDataStore(ctx)
}
```
> 若 core Task 8 尚未实现，直接按此形态写 `DataModule.kt` 即可。

- [x] **Step 4: 改写「下一节课」组件** `ui/widget/NextClassWidget.kt`（替换 Task 1 冒烟版）

```kotlin
package com.scnu.schedule.ui.widget

import android.content.Context
import androidx.compose.runtime.Composable
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp
import androidx.glance.GlanceId
import androidx.glance.appwidget.GlanceAppWidget
import androidx.glance.appwidget.cornerRadius
import androidx.glance.appwidget.provideContent
import androidx.glance.background
import androidx.glance.layout.Box
import androidx.glance.layout.Column
import androidx.glance.layout.Modifier
import androidx.glance.layout.fillMaxSize
import androidx.glance.layout.padding
import androidx.glance.text.FontWeight
import androidx.glance.text.Text
import androidx.glance.text.TextStyle
import androidx.glance.unit.ColorProvider
import com.scnu.schedule.ui.theme.AppPalette
import com.scnu.schedule.ui.theme.AppThemeType
import com.scnu.schedule.ui.theme.ClaudePalette
import com.scnu.schedule.ui.theme.OpenCodePalette
import java.time.LocalDateTime

/** 下一节课 2×1：课程名 + 地点 + 开始时间/倒计时；空态显示「暂无课程」。 */
class NextClassWidget : GlanceAppWidget() {
    override suspend fun provideGlance(context: Context, id: GlanceId) {
        val data = WidgetDataReader.load(context)
        val palette = if (data?.theme == AppThemeType.CLAUDE) ClaudePalette else OpenCodePalette
        val model = data?.let { d ->
            WidgetFormatter.nextClass(
                d.courses,
                d.activeTimetable?.periods ?: emptyList(),
                d.semester,
                LocalDateTime.now(),
            )
        }
        provideContent { NextClassContent(model, palette) }
    }
}

@Composable
fun NextClassContent(model: NextClassModel?, palette: AppPalette) {
    Box(
        Modifier.fillMaxSize()
            .background(ColorProvider(palette.canvas))
            .padding(12.dp)
            .cornerRadius(if (palette.isDark) 0.dp else 12.dp),
    ) {
        if (model == null) {
            Text(
                "暂无课程",
                style = TextStyle(color = ColorProvider(palette.muted), fontSize = 13.sp),
            )
        } else {
            Column(Modifier.fillMaxSize()) {
                Text(
                    model.name,
                    maxLines = 1,
                    style = TextStyle(color = ColorProvider(palette.ink), fontSize = 20.sp, fontWeight = FontWeight.Bold),
                )
                Text(
                    "${model.location} · ${WidgetFormatter.hhmm(model.startMinute)} 开始",
                    maxLines = 1,
                    style = TextStyle(color = ColorProvider(palette.muted), fontSize = 12.sp),
                    modifier = Modifier.padding(top = 4.dp),
                )
                Text(
                    WidgetFormatter.countdownText(model.minutesUntil),
                    style = TextStyle(color = ColorProvider(palette.primary), fontSize = 13.sp),
                    modifier = Modifier.padding(top = 4.dp),
                )
            }
        }
    }
}
```

- [x] **Step 5: 编译验证**

Run: `gradle :app:assembleDebug`
Expected: `BUILD SUCCESSFUL`。
> `[VERIFY]` 点（编译报错时就近修正）：`androidx.glance.layout.Modifier` 导入；`androidx.glance.appwidget.cornerRadius`；`ColorProvider` 的 `androidx.glance.unit` 包；`TextStyle` 的 `color` 参数类型；`Modifier.padding(top = 4.dp)` 的方向性重载；`Text.maxLines` 参数。

- [x] **Step 6: 单测回归**

Run: `gradle :app:testDebugUnitTest`
Expected: PASS（含 Task 2 的 WidgetFormatterTest 全绿）。

- [x] **Step 7: Commit**

```bash
git add -A && git commit -m "feat(widget): 直读主库+DataStore，下一节课 2×1 接入真实数据与主题"
```

---

### Task 4: 今日课程 4×2

**Files:**
- Create: `app/src/main/res/xml/today_widget.xml`
- Modify: `app/src/main/AndroidManifest.xml`
- Create: `app/src/main/java/com/scnu/schedule/ui/widget/TodayWidget.kt`
- Create: `app/src/main/java/com/scnu/schedule/ui/widget/TodayWidgetReceiver.kt`

- [x] **Step 1: 写 provider 配置** `res/xml/today_widget.xml`

```xml
<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_loading"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen" />
```

- [x] **Step 2: Manifest 注册 receiver**

在 `NextClassWidgetReceiver` 之后加：
```xml
        <receiver
            android:name=".ui.widget.TodayWidgetReceiver"
            android:label="@string/widget_name_today"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_widget" />
        </receiver>
```

- [x] **Step 3: 写组件** `ui/widget/TodayWidget.kt`

```kotlin
package com.scnu.schedule.ui.widget

import android.content.Context
import androidx.compose.runtime.Composable
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp
import androidx.glance.GlanceId
import androidx.glance.appwidget.GlanceAppWidget
import androidx.glance.appwidget.provideContent
import androidx.glance.background
import androidx.glance.layout.Column
import androidx.glance.layout.Modifier
import androidx.glance.layout.Row
import androidx.glance.layout.fillMaxSize
import androidx.glance.layout.fillMaxWidth
import androidx.glance.layout.padding
import androidx.glance.layout.weight
import androidx.glance.text.FontWeight
import androidx.glance.text.Text
import androidx.glance.text.TextStyle
import androidx.glance.unit.ColorProvider
import com.scnu.schedule.ui.theme.AppPalette
import com.scnu.schedule.ui.theme.AppThemeType
import com.scnu.schedule.ui.theme.ClaudePalette
import com.scnu.schedule.ui.theme.OpenCodePalette
import java.time.LocalDate

/** 今日课程 4×2：节次时间段 + 名称 + 地点；空态显示「今天没有课」。 */
class TodayWidget : GlanceAppWidget() {
    override suspend fun provideGlance(context: Context, id: GlanceId) {
        val data = WidgetDataReader.load(context)
        val palette = if (data?.theme == AppThemeType.CLAUDE) ClaudePalette else OpenCodePalette
        val today = LocalDate.now()
        val model = data?.let { d ->
            TodayWidgetModel(
                dateLabel = WidgetFormatter.dateLabel(today),
                entries = WidgetFormatter.todayEntries(
                    d.courses,
                    d.activeTimetable?.periods ?: emptyList(),
                    d.semester,
                    today,
                ),
            )
        } ?: TodayWidgetModel("", emptyList())
        provideContent { TodayContent(model, palette) }
    }
}

@Composable
fun TodayContent(model: TodayWidgetModel, palette: AppPalette) {
    Column(
        Modifier.fillMaxSize()
            .background(ColorProvider(palette.canvas))
            .padding(10.dp),
    ) {
        Text(
            "今日 · ${model.dateLabel}",
            style = TextStyle(color = ColorProvider(palette.muted), fontSize = 11.sp),
        )
        if (model.entries.isEmpty()) {
            Text(
                "今天没有课",
                style = TextStyle(color = ColorProvider(palette.muted), fontSize = 13.sp),
                modifier = Modifier.padding(top = 8.dp),
            )
        } else {
            model.entries.take(4).forEach { e ->
                Row(Modifier.fillMaxWidth().padding(top = 6.dp)) {
                    Text(
                        WidgetFormatter.hhmm(e.startMinute),
                        style = TextStyle(color = ColorProvider(palette.primary), fontSize = 11.sp, fontWeight = FontWeight.Bold),
                        modifier = Modifier.weight(0.3f),
                    )
                    Column(Modifier.weight(1f)) {
                        Text(e.name, maxLines = 1,
                            style = TextStyle(color = ColorProvider(palette.ink), fontSize = 12.sp))
                        Text(e.location, maxLines = 1,
                            style = TextStyle(color = ColorProvider(palette.muted), fontSize = 10.sp))
                    }
                }
            }
        }
    }
}
```

- [x] **Step 4: 写 receiver** `ui/widget/TodayWidgetReceiver.kt`

```kotlin
package com.scnu.schedule.ui.widget

import androidx.glance.appwidget.GlanceAppWidget
import androidx.glance.appwidget.GlanceAppWidgetReceiver

class TodayWidgetReceiver : GlanceAppWidgetReceiver() {
    override val glanceAppWidget: GlanceAppWidget = TodayWidget()
}
```

- [x] **Step 5: 编译验证**

Run: `gradle :app:assembleDebug`
Expected: `BUILD SUCCESSFUL`。
> `[VERIFY]`：`androidx.glance.layout.weight` 的 `Modifier.weight(Float)` 扩展（若为 RowScope 成员则导入多余但无害）。

- [x] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(widget): 今日课程 4×2 组件"
```

---

### Task 5: 周课表 4×3（单双周过滤）

**Files:**
- Create: `app/src/main/res/xml/week_widget.xml`
- Modify: `app/src/main/AndroidManifest.xml`
- Create: `app/src/main/java/com/scnu/schedule/ui/widget/WeekWidget.kt`
- Create: `app/src/main/java/com/scnu/schedule/ui/widget/WeekWidgetReceiver.kt`

- [x] **Step 1: 写 provider 配置** `res/xml/week_widget.xml`

```xml
<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="180dp"
    android:targetCellWidth="4"
    android:targetCellHeight="3"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_loading"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen" />
```

- [x] **Step 2: Manifest 注册 receiver**

在 `TodayWidgetReceiver` 之后加：
```xml
        <receiver
            android:name=".ui.widget.WeekWidgetReceiver"
            android:label="@string/widget_name_week"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/week_widget" />
        </receiver>
```

- [x] **Step 3: 写组件** `ui/widget/WeekWidget.kt`

```kotlin
package com.scnu.schedule.ui.widget

import android.content.Context
import androidx.compose.runtime.Composable
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp
import androidx.glance.GlanceId
import androidx.glance.appwidget.GlanceAppWidget
import androidx.glance.appwidget.provideContent
import androidx.glance.background
import androidx.glance.layout.Box
import androidx.glance.layout.Column
import androidx.glance.layout.Modifier
import androidx.glance.layout.Row
import androidx.glance.layout.fillMaxSize
import androidx.glance.layout.fillMaxWidth
import androidx.glance.layout.height
import androidx.glance.layout.padding
import androidx.glance.layout.weight
import androidx.glance.text.Text
import androidx.glance.text.TextStyle
import androidx.glance.text.style.TextAlign
import androidx.glance.unit.ColorProvider
import com.scnu.schedule.ui.theme.AppPalette
import com.scnu.schedule.ui.theme.AppThemeType
import com.scnu.schedule.ui.theme.ClaudePalette
import com.scnu.schedule.ui.theme.OpenCodePalette
import java.time.LocalDate

/** 周课表 4×3：迷你 7 列周网格，仅渲染当前周有课（含单双周过滤）。 */
class WeekWidget : GlanceAppWidget() {
    override suspend fun provideGlance(context: Context, id: GlanceId) {
        val data = WidgetDataReader.load(context)
        val palette = if (data?.theme == AppThemeType.CLAUDE) ClaudePalette else OpenCodePalette
        val model = data?.let { d ->
            WidgetFormatter.weekGrid(d.courses, d.semester, LocalDate.now())
        } ?: WeekGridModel(emptyList())
        provideContent { WeekContent(model, palette) }
    }
}

@Composable
fun WeekContent(model: WeekGridModel, palette: AppPalette) {
    Column(
        Modifier.fillMaxSize()
            .background(ColorProvider(palette.canvas))
            .padding(6.dp),
    ) {
        if (model.days.all { it.courses.isEmpty() }) {
            Text(
                "本周暂无课程",
                style = TextStyle(color = ColorProvider(palette.muted), fontSize = 13.sp),
            )
        } else {
            // 表头：一~日
            Row(Modifier.fillMaxWidth()) {
                model.days.forEach { d ->
                    Text(
                        d.label,
                        style = TextStyle(color = ColorProvider(palette.muted), fontSize = 9.sp, textAlign = TextAlign.Center),
                        modifier = Modifier.weight(1f),
                    )
                }
            }
            // 3 行课程格子
            repeat(3) { r ->
                Row(Modifier.fillMaxWidth().padding(top = 2.dp)) {
                    model.days.forEach { day ->
                        val cell = day.courses.getOrNull(r)
                        Box(
                            Modifier.weight(1f).height(26.dp)
                                .then(
                                    if (cell != null) {
                                        Modifier.background(ColorProvider(palette.coursePalette[cell.colorIndex % palette.coursePalette.size]))
                                    } else {
                                        Modifier.background(ColorProvider(palette.surfaceSoft))
                                    }
                                ),
                        ) {
                            if (cell != null) {
                                Text(
                                    cell.name,
                                    maxLines = 1,
                                    style = TextStyle(color = ColorProvider(palette.ink), fontSize = 8.sp, textAlign = TextAlign.Center),
                                    modifier = Modifier.padding(2.dp),
                                )
                            }
                        }
                    }
                }
            }
        }
    }
}
```

- [x] **Step 4: 写 receiver** `ui/widget/WeekWidgetReceiver.kt`

```kotlin
package com.scnu.schedule.ui.widget

import androidx.glance.appwidget.GlanceAppWidget
import androidx.glance.appwidget.GlanceAppWidgetReceiver

class WeekWidgetReceiver : GlanceAppWidgetReceiver() {
    override val glanceAppWidget: GlanceAppWidget = WeekWidget()
}
```

- [x] **Step 5: 编译验证**

Run: `gradle :app:assembleDebug`
Expected: `BUILD SUCCESSFUL`。
> `[VERIFY]`：`androidx.glance.text.style.TextAlign`（`TextAlign.Center`）；`Modifier.then` 链式拼接；`Modifier.height(Dp)`。

- [x] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(widget): 周课表 4×3 迷你网格（单双周过滤）"
```

---

### Task 6: 刷新机制（数据变更广播 + 统一刷新）

课程/作息/学期/主题变动后 App 发广播 → `WidgetRefreshReceiver` 收到 → 统一刷新所有已放置实例。`updatePeriodMillis`（30 分钟）兜底系统时间推进。

**Files:**
- Create: `app/src/main/java/com/scnu/schedule/ui/widget/WidgetUpdateNotifier.kt`
- Create: `app/src/main/java/com/scnu/schedule/ui/widget/WidgetRefreshReceiver.kt`
- Modify: `app/src/main/AndroidManifest.xml`
- Modify: `app/src/main/java/com/scnu/schedule/data/repository/CourseRepositoryImpl.kt`
- Modify: `app/src/main/java/com/scnu/schedule/data/repository/TimeTableRepositoryImpl.kt`
- Modify: `app/src/main/java/com/scnu/schedule/data/repository/SettingsRepositoryImpl.kt`
- Modify: `app/src/main/java/com/scnu/schedule/ScheduleApp.kt`

- [x] **Step 1: 写刷新通知器** `ui/widget/WidgetUpdateNotifier.kt`

```kotlin
package com.scnu.schedule.ui.widget

import android.content.Context
import android.content.Intent
import androidx.glance.appwidget.GlanceAppWidgetManager
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.SupervisorJob
import kotlinx.coroutines.launch

/** 数据变更统一刷新：App 侧调用 [notifyDataChanged]，广播触发 [WidgetRefreshReceiver]。 */
object WidgetUpdateNotifier {
    const val ACTION_REFRESH = "com.scnu.schedule.action.WIDGET_REFRESH"

    /** 课程/作息/学期/主题变更后调用；无组件实例时广播是空操作，安全。 */
    fun notifyDataChanged(context: Context) {
        context.sendBroadcast(Intent(ACTION_REFRESH).setPackage(context.packageName))
    }

    /** 刷新所有已放置的组件实例（在协程里逐个 update）。 */
    fun updateAllNow(context: Context) {
        val scope = CoroutineScope(SupervisorJob() + Dispatchers.IO)
        scope.launch {
            val manager = GlanceAppWidgetManager(context)
            manager.getGlanceIds(NextClassWidget::class.java).forEach { NextClassWidget().update(context, it) }
            manager.getGlanceIds(TodayWidget::class.java).forEach { TodayWidget().update(context, it) }
            manager.getGlanceIds(WeekWidget::class.java).forEach { WeekWidget().update(context, it) }
        }
    }
}
```
> `[VERIFY]`：`GlanceAppWidgetManager(context).getGlanceIds(Class)`（suspend）与 `GlanceAppWidget.update(context, glanceId)`（suspend）的签名。若 `getGlanceIds` 不可用，可改回 `NextClassWidget().updateAll(context)`（`updateAll` 为 GlanceAppWidget 成员）。

- [x] **Step 2: 写刷新接收器** `ui/widget/WidgetRefreshReceiver.kt`

```kotlin
package com.scnu.schedule.ui.widget

import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent

class WidgetRefreshReceiver : BroadcastReceiver() {
    override fun onReceive(context: Context, intent: Intent) {
        if (intent.action == WidgetUpdateNotifier.ACTION_REFRESH) {
            WidgetUpdateNotifier.updateAllNow(context)
        }
    }
}
```

- [x] **Step 3: Manifest 注册刷新接收器**

在 `WeekWidgetReceiver` 之后加：
```xml
        <receiver
            android:name=".ui.widget.WidgetRefreshReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.scnu.schedule.action.WIDGET_REFRESH" />
            </intent-filter>
        </receiver>
```

- [x] **Step 4: 改 `CourseRepositoryImpl`**（注入 Context，写后发广播）

```kotlin
package com.scnu.schedule.data.repository

import android.content.Context
import com.scnu.schedule.data.db.CourseDao
import com.scnu.schedule.data.db.CourseEntity
import com.scnu.schedule.domain.model.Course
import com.scnu.schedule.domain.repo.CourseRepository
import com.scnu.schedule.ui.widget.WidgetUpdateNotifier
import dagger.hilt.android.qualifiers.ApplicationContext
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.flow.map
import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class CourseRepositoryImpl @Inject constructor(
    private val dao: CourseDao,
    @ApplicationContext private val context: Context,
) : CourseRepository {
    override val courses: Flow<List<Course>> = dao.observeAll().map { list ->
        list.map { Course(it.id, it.name, it.teacher, it.location, it.dayOfWeek, it.startPeriod, it.endPeriod, it.weekPattern, it.colorIndex) }
    }

    override suspend fun upsert(course: Course) {
        dao.upsert(CourseEntity(course.id, course.name, course.teacher, course.location, course.dayOfWeek, course.startPeriod, course.endPeriod, course.weekPattern, course.colorIndex))
        WidgetUpdateNotifier.notifyDataChanged(context)
    }

    override suspend fun delete(id: Long) {
        dao.delete(id)
        WidgetUpdateNotifier.notifyDataChanged(context)
    }
}
```

- [x] **Step 5: 改 `TimeTableRepositoryImpl`**

```kotlin
package com.scnu.schedule.data.repository

import android.content.Context
import com.scnu.schedule.data.db.PeriodEntity
import com.scnu.schedule.data.db.TimeTableDao
import com.scnu.schedule.data.db.TimeTableEntity
import com.scnu.schedule.data.db.TimeTableWithPeriods
import com.scnu.schedule.domain.model.Period
import com.scnu.schedule.domain.model.TimeTable
import com.scnu.schedule.domain.repo.TimeTableRepository
import com.scnu.schedule.ui.widget.WidgetUpdateNotifier
import dagger.hilt.android.qualifiers.ApplicationContext
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.flow.map
import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class TimeTableRepositoryImpl @Inject constructor(
    private val dao: TimeTableDao,
    @ApplicationContext private val context: Context,
) : TimeTableRepository {
    override val timetables: Flow<List<TimeTable>> = dao.observeAll().map { rows ->
        rows.map { TimeTable(it.timetable.id, it.timetable.name, it.timetable.isDefault,
            it.periods.sortedBy { p -> p.periodIndex }.map { Period(it.timetable.id, it.periodIndex, it.startMinute, it.endMinute) }) }
    }

    override suspend fun upsert(timetable: TimeTable) {
        val ttId = dao.upsertTimetable(TimeTableEntity(timetable.id, timetable.name, timetable.isDefault))
        dao.deletePeriods(ttId)
        dao.upsertPeriods(timetable.periods.map { PeriodEntity(0, ttId, it.periodIndex, it.startMinute, it.endMinute) })
        WidgetUpdateNotifier.notifyDataChanged(context)
    }

    override suspend fun delete(id: Long) {
        dao.deletePeriods(id)
        dao.deleteTimetable(id)
        WidgetUpdateNotifier.notifyDataChanged(context)
    }

    /** 首次启动无作息时，种入石牌校区默认 10 节 */
    suspend fun ensureDefaultSeeded() {
        if (dao.count() == 0) {
            val ttId = dao.upsertTimetable(TimeTableEntity(name = "石牌校区", isDefault = true))
            val mins = listOf(
                1 to (510 to 550), 2 to (560 to 600), 3 to (620 to 660), 4 to (670 to 710),
                5 to (870 to 910), 6 to (920 to 960), 7 to (970 to 1010), 8 to (1020 to 1060),
                9 to (1140 to 1180), 10 to (1190 to 1230),
            )
            dao.upsertPeriods(mins.map { (idx, t) -> PeriodEntity(0, ttId, idx, t.first, t.second) })
            WidgetUpdateNotifier.notifyDataChanged(context)
        }
    }
}
```

- [x] **Step 6: 改 `SettingsRepositoryImpl`**

```kotlin
package com.scnu.schedule.data.repository

import android.content.Context
import com.scnu.schedule.data.prefs.SettingsDataStore
import com.scnu.schedule.domain.model.Semester
import com.scnu.schedule.domain.repo.SettingsRepository
import com.scnu.schedule.ui.theme.AppThemeType
import com.scnu.schedule.ui.widget.WidgetUpdateNotifier
import dagger.hilt.android.qualifiers.ApplicationContext
import kotlinx.coroutines.flow.Flow
import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class SettingsRepositoryImpl @Inject constructor(
    private val store: SettingsDataStore,
    @ApplicationContext private val context: Context,
) : SettingsRepository {
    override val theme: Flow<AppThemeType> get() = store.theme
    override val semester: Flow<Semester> get() = store.semester
    override val activeTimeTableId: Flow<Long?> get() = store.activeTimeTableId

    override suspend fun setTheme(type: AppThemeType) {
        store.setTheme(type)
        WidgetUpdateNotifier.notifyDataChanged(context)
    }

    override suspend fun setSemester(semester: Semester) {
        store.setSemester(semester)
        WidgetUpdateNotifier.notifyDataChanged(context)
    }

    override suspend fun setActiveTimeTable(id: Long?) {
        store.setActiveTimeTable(id)
        WidgetUpdateNotifier.notifyDataChanged(context)
    }
}
```

- [x] **Step 7: 改 `ScheduleApp`**（种子后发一次刷新）

```kotlin
package com.scnu.schedule

import android.app.Application
import com.scnu.schedule.data.repository.TimeTableRepositoryImpl
import com.scnu.schedule.ui.widget.WidgetUpdateNotifier
import dagger.hilt.android.HiltAndroidApp
import javax.inject.Inject
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.launch

@HiltAndroidApp
class ScheduleApp : Application() {
    @Inject lateinit var timeTableRepository: TimeTableRepositoryImpl
    private val scope = CoroutineScope(Dispatchers.IO)

    override fun onCreate() {
        super.onCreate()
        scope.launch {
            timeTableRepository.ensureDefaultSeeded()
            WidgetUpdateNotifier.notifyDataChanged(applicationContext)
        }
    }
}
```

- [x] **Step 8: 编译验证**

Run: `gradle :app:assembleDebug`
Expected: `BUILD SUCCESSFUL`。
> 说明：`data/repository` 依赖 `ui.widget.WidgetUpdateNotifier` 属「数据层 → UI 层」倒置。MVP 可接受的务实取舍（通知器不持有 UI 状态、仅发广播）；若后续要消除，可把通知器挪到独立 `data/widget` 包或改为在 ViewModel 层调用，见 Self-Review。

- [x] **Step 9: 全量单测回归**

Run: `gradle :app:testDebugUnitTest`
Expected: PASS（WeekPattern / WeekCalculator / CourseOverlap / DaoTest / WidgetFormatterTest 全绿）。

- [x] **Step 10: Commit**

```bash
git add -A && git commit -m "feat(widget): 数据变更广播刷新 + Repository 注入 + 启动种子刷新"
```

---

## Self-Review 记录

**Spec 覆盖核对（设计文档 §7）：**
- 下一节课 2×1：Task 1/3（课程名 + 地点 + 开始时间/倒计时）。✓
- 今日课程 4×2：Task 4（节次时间段 + 名称 + 地点）。✓
- 周课表 4×3：Task 5（迷你周网格、仅当前周有课、单双周过滤 → `WidgetFormatter.weekGrid` 用 `weekPattern.contains(week)`）。✓
- 数据直读 Room + DataStore（主题）：Task 3 `WidgetDataReader`。✓
- 刷新：Task 6 广播 + `updatePeriodMillis`（30 分钟兜底）。✓
- 主题跟随（Claude/OpenCode）：Task 3-5 均读 `settings.theme` → `ClaudePalette`/`OpenCodePalette` 手动取色（未用 GlanceTheme，因主题固定两套，手动色更简单可控）。✓
- 空态：三个组件均有占位文案（「暂无课程」「今天没有课」「本周暂无课程」）；`WidgetDataReader.load` 失败返回 null，组件不崩溃。✓
- §8 错误处理：数据缺失显示占位，不崩溃。✓

**数据源决策：** 直读主库（`DbProvider` 单例），未建独立 widget 库；理由见 Plan 头部「数据来源决策」，与拾光的独立库 + 同步器形成对照。

**TDD 覆盖：** Glance UI 无法 JVM 单测，故纯逻辑 `WidgetFormatter` 走 TDD（Task 2：先失败后绿，11 个测试覆盖 nextClass/todayEntries/weekGrid/单双周/倒计时文案/时间与日期格式化）；组件层以 `assembleDebug` 编译冒烟验证。

**占位符扫描：** 无 TBD/TODO 悬空；倒计时按分钟 tick 仅作为可选增强说明（updatePeriodMillis 下限 30 分钟，MVP 接受；如需精确到分钟，可在 `NextClassWidgetReceiver.onEnabled` 用 AlarmManager 每分钟发 `ACTION_REFRESH`，`onDisabled` 取消，属后置优化）。

**[VERIFY] 清单（控制器核对）：**
1. Glance 1.1.1 的 `Modifier` 导入包：计划用 `androidx.glance.layout.Modifier`（1.1.x 中 `androidx.glance.Modifier` 为新家，`layout.Modifier` 为兼容别名，两者皆可）。
2. `ColorProvider`：`androidx.glance.unit.ColorProvider(color: androidx.compose.ui.graphics.Color)`，配合 `androidx.glance.background(ColorProvider)`。
3. `Modifier.cornerRadius(Dp)`：`androidx.glance.appwidget.cornerRadius`。
4. `androidx.glance.text.FontWeight`（Bold/Normal）与 `androidx.glance.text.style.TextAlign`（Center）。
5. `Modifier.weight(Float)`：`androidx.glance.layout.weight`（RowScope/ColumnScope 扩展或成员）。
6. `Modifier.then(...)` 与方向性 `Modifier.padding(top = 4.dp)` 重载。
7. `Text.maxLines` 参数是否存在。
8. `GlanceAppWidgetManager(context).getGlanceIds(Clazz)`（suspend）与 `GlanceAppWidget.update(context, glanceId)`（suspend）；不可用则退化为 `GlanceAppWidget().updateAll(context)`。
9. `res/xml` 中 `initialLayout="@layout/widget_loading"` 用自定义占位布局（不依赖库自带 `glance_initial_layout`，规避资源可见性问题）。

**类型一致性：** `NextClassModel`/`TodayEntry`/`TodayWidgetModel`/`CourseCell`/`DayColumn`/`WeekGridModel` 由 `WidgetFormatter` 产出、三个组件消费，字段命名一致；`WidgetData` 与 `WidgetDataReader` 一致；复用 core 的 `WeekCalculator.currentWeek`、`AppThemeType`、`ClaudePalette`/`OpenCodePalette`、`SettingsDataStore`、`Course/Period/Semester/TimeTable` domain 模型，与 core Plan 的类型签名完全一致。
